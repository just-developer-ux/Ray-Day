let banner = ["Test №1", "Test №2", "Test №3"];

// console.log(banner[1].length);


for (let i = 0; i < banner.length; i++) {
   const bannerLenght = banner[i].length;

   for (let ii = 0; ii < bannerLenght; ii++) {
      // const element = array[ii];
      // bannerF();

   }

}

function bannerF() {

}

let vievBanner = ["", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", "", ""];
console.log(vievBanner);





// let cloud = document.querySelectorAll('.balloon_clouds')
// for (let i = 0; i < cloud.length; i++) {
//    let element = cloud[i];
//    let delay = Math.random() * 5;
//    element.style.animationDelay = "" + delay + "s";

// }

























//непотрібне но к памь бо хз чого
// const cards = document.querySelectorAll('.block_hours_cards');

// for (let i = 0; i < cards.length; i++) {
//    const card = cards[i];
//    card.addEventListener('mousemove', startRotate, true);
//    card.addEventListener('mouseout', stopRotate, true);
// }

// function startRotate(event) {
//    const cardItem = this.querySelector('.hours_cards');
//    const halfHeight = cardItem.offsetHeight / 2;
//    const halfWidth = cardItem.offsetWidth / 2;

//    cardItem.style.transform = 'rotateX(' + -(event.offsetY - halfHeight) / 10 + 'deg) rotateY(' + (event.offsetX - halfWidth) / 5 + 'deg)';
// }

// function stopRotate(event) {
//    const cardItem = this.querySelector('.hours_cards');
//    cardItem.style.transform = 'rotate(0)'
// }














































